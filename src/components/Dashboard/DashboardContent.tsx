import { Divider, Stack, Typography } from "@mui/material";

const DashboardContent = () => {
	return <Stack p={1} flexGrow={1} gap={1}>
		<Typography variant="h5">
			Vendas
		</Typography>
	</Stack>
}

export default DashboardContent;